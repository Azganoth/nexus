{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["generate"],
      "outputs": ["dist/**"]
    },
    "generate": {
      "inputs": ["$TURBO_DEFAULT$", ".env", "prisma/"],
      "outputs": ["generated/**"]
    }
  }
}
